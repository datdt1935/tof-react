export enum DrawType {
  LineIn = 'Line in',
  LineOut = 'Line out',
  LineInAndOut = 'Line in/out',
  ZoneFloor = 'Floor',
  ZoneExclusion = 'Exclusion',
  ZoneMark = 'Mark',
}

export enum BackgroundType {
  Image = 1,
  Video = 2,
}
